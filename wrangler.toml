name = "grupogurdjieff-cl"
compatibility_date = "2024-01-01"

# Pages configuration
pages_build_output_dir = "."

# Custom domain configuration (optional)
# routes = [
#   { pattern = "grupogurdjieff.chuchurex.cl", custom_domain = true }
# ]

[site]
bucket = "."

# Environment variables (use .env for sensitive data)
# [vars]
# PROJECT_NAME = "grupogurdjieff-cl"
